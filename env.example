# Environment variables for Docker Compose
# Copy this file to .env and modify the ports if needed

# Backend port (default: 5000)
BACKEND_PORT=5000

# Frontend port (default: 3000)
FRONTEND_PORT=3000

# Example: If port 5000 is in use, change to:
# BACKEND_PORT=5001
# FRONTEND_PORT=3001
