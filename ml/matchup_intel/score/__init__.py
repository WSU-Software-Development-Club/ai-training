"""Layer 2 — small specialized scorers for quantitative factors.

These output calibrated magnitudes for factors where a trained/deterministic
scorer beats an LLM's vibe (weather, rest/travel, line-play mismatch, tempo).
The LLM never judges magnitude for these.
"""
